import React, {FC} from 'react';
import { StyleSheet, Text, View } from 'react-native';

const AddCryptoScreen: FC = () => {
  return (
    <View>
      <Text>Add new crypto Screen</Text>
    </View>
  );
};

export default AddCryptoScreen;

const styles = StyleSheet.create({});
